ci:
  enable_tests: true
  services: |
      services:
            redis:
              image: redis
              ports:
                - 6379:6379
              options: --entrypoint redis-server
  env:
    entries: |
      REDIS_HOSTNAME: "redis"
